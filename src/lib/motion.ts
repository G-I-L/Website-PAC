// Matches the easing curves used on the original People Axis brand site
// (cubic-bezier(.22,.7,.2,1) for reveals, cubic-bezier(0.16,1,0.3,1) for
// interactive/hover motion) — the same "expo-out" feel used across most
// premium marketing sites (Zoho, Webflow templates, etc).
export const EASE = [0.22, 0.7, 0.2, 1] as const;
export const EASE_STRONG = [0.16, 1, 0.3, 1] as const;
